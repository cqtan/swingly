import {
  setCurrentUser,
  signInWithProvider,
  signOut,
  signUp,
  signInWithEmail,
  editUser,
  deleteUser
} from '../user.actions';

describe('User Actions', () => {

  it('setCurrentUser', () => {
    expect(true).toBe(true);
  });
});